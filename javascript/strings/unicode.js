/* javascript uses unicode for its characters. */

/* normally, the encodings for characters are two bytes long. */
/* however, if the characters are rare, their encodings may be four bytes long. */
/* these four-byte characters are called 'surrogate pairs'. */